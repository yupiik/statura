{
  "apiVersion": "v1",
  "kind": "ConfigMap",
  "metadata": {
    "name": "{{statura.configmap.name:-{{statura.name:-statura}}}}",
    "namespace": "{{statura.namespace:-statura}}",
    "labels": {{statura.configmap.labels:-{{statura.labels:-{} }} }},
    "annotations": {{statura.configmap.annotations:-{{statura.annotations:-{} }} }}
  },
  "data": {{statura.configuration.content}}
}
