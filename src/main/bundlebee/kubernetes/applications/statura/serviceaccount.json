{
  "apiVersion": "v1",
  "kind": "ServiceAccount",
  "metadata": {
    "name": "{{statura.serviceAccount.name:-{{statura.name:-statura}}}}",
    "namespace": "{{statura.namespace:-statura}}",
    "labels": {{statura.serviceAccount.labels:-{{statura.labels:-{} }} }},
    "annotations": {{statura.serviceAccount.annotations:-{{statura.annotations:-{} }} }}
  },
  "automountServiceAccountToken": {{statura.serviceAccount.automount:-false}}
}
