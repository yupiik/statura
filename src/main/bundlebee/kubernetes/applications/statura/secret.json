{
  "apiVersion": "v1",
  "kind": "Secret",
  "metadata": {
    "name": "{{statura.secret.name:-{{statura.name:-statura}}}}",
    "namespace": "{{statura.namespace:-statura}}",
    "labels": {{statura.secret.labels:-{{statura.labels:-{} }} }},
    "annotations": {{statura.secret.annotations:-{{statura.annotations:-{} }} }}
  },
  "type": "Opaque",
  "data": {{statura.secret.content:-}}
}
