{
  "auths": {
    "{{registry.key}}": {
      "username": "{{registry.username}}",
      "password": "{{registry.password}}",
      "email": "{{registry.username}}@mail.com",
      "auth": "{{bundlebee-base64:{{registry.username}}:{{registry.password}}}}"
    }
  }
}
