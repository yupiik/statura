= Deployment
:minisite-index: 400
:minisite-index-title: Deployment
:minisite-index-description: How to deploy.
:minisite-index-icon: rocket
:minisite-keywords: configuration, documentation
:minisite-breadcrumb: Home[index.html] > Deployment
:minisite-nav-prev-label: Cli
:minisite-nav-prev: cli/index.html
:minisite-nav-next-label: BundleBee

[abstract]
Yupiik chords is a simple Java application, it has several flavors but all are based on Java capabilities.

== Standalone

It is a matter of creating the right java command: `java -cp $classpath io.yupiik.fusion.framework.api.main.CliLauncher $args`.

The classpath is composed of these dependencies:

include::{partialsdir}/generated/dependencies.core.adoc[]

So it can look to:

[source,bash]
----
include::{partialsdir}/generated/dependencies.sample-command.adoc[]
----

or if you put all libraries in `lib/` folder: `java -cp lib/*.jar com.tmf.catalog.Tmf620ApiProductCatalogApplication $args`.

TIP: all libraries are available on maven link:https://repo.maven.apache.org/maven2/io/yupiik[central].

== Yupiik BundleBee

You can use Yupiik link:https://www.yupiik.io/bundlebee/[BundleBee] to package and make your deployment configurable.
Project provides out of the box an xref:bundlebee.adoc[alveolus] for BundleBee.
